declare and retain 
	b1 as boolean;
	b2 as boolean;
	s1 as square;
        i1 as integer;
    fct1 as function():square
    	do
    		compute i1 
    		set i1 to i1 + 1 % longitude + grid size - i1
    	done 
when your turn 
by default 
    do
    	compute fct1()
    done