declare and retain 
	b1 as boolean;
	b2 as boolean;
	b3 as boolean;
        i1 as integer;
        s1 as square;
    fct1 as function():square
    	do
    		compute i1 
    		set i1 to i1 + 1 % longitude + grid size - i1
    	done 
 
when your turn 
by default 
    do
    	compute fct1()
    	set s1 to ammo
    done