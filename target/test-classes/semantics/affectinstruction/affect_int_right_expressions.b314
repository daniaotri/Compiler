/*

0

0,0,0

0,0,0,0,0
*/
declare and retain 
    b as boolean;
    i as integer;
    s as square;
when your turn
by default
    do 
        set i to 0 + 42 * 1
        set i to 42 + latitude % 1 + i
        set i to (-34 - 2) * grid size - i 
        set i to map count - radio count + ammo count % 2 - soda count
    done

