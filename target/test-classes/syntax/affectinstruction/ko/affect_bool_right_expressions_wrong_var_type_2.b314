/* My comments */ 
declare and retain 
    b as boolean;
    i as integer;
    s as square;
when your turn
by default
    do 
        set b to true and false or true
        set b to not false and 0 > 45 + 3 or s = player
        set b to graal is north or (b and nearby[1,1] = zombie)
        set s to ennemi is north and not b /* Wrong type */
        set i to 0 + 42 * 1
        set i to 42 + latitude % life + i
        set i to (-34 - 2) * grid size - i 
        set i to map count - radio count + ammo count % fruits count - soda count
    done

