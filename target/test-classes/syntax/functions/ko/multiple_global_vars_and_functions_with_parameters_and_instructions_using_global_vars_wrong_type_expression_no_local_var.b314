/* My comments */ 
declare and retain 
	b1 as boolean;
	b2 as boolean;
	b3 as boolean;
    fct1 as function(x as integer):square
    	do
    		compute x + 1
    		set x to x + 1 % longitude + grid size - i1
    		set fct1 to s2
    	done 
    i1 as integer; 
	b4 as boolean;
	b5 as boolean;
    fct2 as function(x as integer, y as boolean):integer
    	do
    		skip
    	done 
    fct3 as function():boolean
    	do
    		set fct3 to true and false or not graal is north or b1 and b2
    	done 
	s1 as square;
	i2 as integer; 
	b6 as boolean;
    i3 as integer; 
    s2 as square;
    fct4 as function():square
    	do
    		skip
    	done 
    fct5 as function(x as integer, y as boolean, z as square):integer
    	do
    		if y and x = 2 then
    			while s2 < i3 do /* wrong type */
    				set x to x + 1
    			done
    		else
    			if z = ammo then
    				next do nothing
    			done
    		done
    	done 
when your turn 
by default 
    do skip done